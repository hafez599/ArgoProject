apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: web  # <-- Crucial line! Tells Kustomize which container to apply this to
